# Completeness critique of the seven rebuild-foundation investigations

Critic report, read-only. Written 2026-09-26 ~03:00 KST on the Mac mini. Inputs: the seven reports `prototype.md`, `contracts.md`, `code-assets.md`, `design-system.md`, `governance.md`, `mess-diagnosis.md`, `git-deploy.md` in `/private/tmp/claude-501/-Users-woohyeon-Local-ViveTest/2d571f76-63eb-4c2a-b15f-e1060531c343/scratchpad/understand/` (all read in full), plus `peer-handoff-2026-09-26.md` in the same folder. I checked claims against the primary checkout at `main` = `20f973a` (clean; only read-only `git show/grep/ls-tree/log/for-each-ref/tag/rev-parse/cat-file/check-ignore`), the prototype scratchpad (stat and grep only), the live transcripts of the parent session `2d571f76…` and the prototype session `7b610dca…` (read only), the project memory folder, GitHub (`gh repo view`, `gh api` GETs), npm (`npm view next`), and production (`curl` GETs). Nothing was written anywhere except this file.

Abbreviations: `DLG` = `…/proto-session/dialog.md`; `MOCK/` = `/private/tmp/claude-501/-Users-woohyeon-Local-ViveTest/7b610dca-70f4-41bf-b48e-fe44caf07c81/scratchpad/mockups/`; `PARENT` = `/Users/woohyeon/.claude/projects/-Users-woohyeon-Local-ViveTest/2d571f76-63eb-4c2a-b15f-e1060531c343.jsonl`; `PROTO-T` = `/Users/woohyeon/.claude/projects/-Users-woohyeon-Local-ViveTest/7b610dca-70f4-41bf-b48e-fe44caf07c81.jsonl`.

---

## 0. Headline findings — read these first

1. **The owner sent a follow-up after every report was written, and it overturns the reports' carry policy.** The seven reports have mtimes 17:44:08Z–17:49:44Z. At **2026-09-25T17:52:19Z** (02:52 KST) the owner wrote in the parent session (PARENT, user message, verbatim): 「이번 요청의 근본 목적은 변경된 모든 누적사항과 결정된 프로토타이핑을 from-scratch 원점에서부터 구현해서 이전에 누적된 여러 지저분한 코드 없이 깨끗하고 효율적인 코드구조와 설계를 갖추는 것입니다. 따라서 이전에 구현 레거시는 얽매이지 말고 오로지 프로토타이핑과 요구사항 2개만 살펴보고 밑바탕을 마련해주세요.」 and 「디자인은 병렬 세션에서의 프로토타이핑을 기본으로 하되, 이 디자인과 인터랙션을 바탕으로 구현하는 결과에는 기존 레거시의 요구사항을 담아야합니다. 그리고 이 요구사항을 디자인/프로토타이핑과 결합할 때에는 기존 레거시에서 여러 누적된 방어코드가 없이 깨끗하고 깔끔하고 효율적인 구조로 요구사항이 구현되기를 희망합니다.」 The parent session recorded this in memory as `vivetest-rebuild-inputs-are-prototype-and-requirements.md` (created 02:52 KST). Consequence: `code-assets.md` §9–§10 (carry 107 legacy files verbatim with `git checkout 20f973a -- …`), `governance.md` §5.3/§5.8 (carry guards, seed a 55-entry lessons ledger) and `mess-diagnosis.md` Rule 1 ("`main` receives an explicit port list") no longer fit the owner's stated intent. `contracts.md` §8 (restate normative rules into new SSOT documents) fits it. The plan must reclassify all "FOUNDATION / carry verbatim" verdicts as "reference for re-deriving requirements".
2. **The `next/og` advisory is real, and it matters for legacy production too, not only for the rebuild.** Every report calls it unverified. GitHub advisory **GHSA-vcvr-r3jv-pc5j** (critical, "Remote Code Execution in next/og ImageResponse", published 2026-09-22, affected `>= 16.2.0 < 16.3.6`, patched `16.3.6`) exists, and `16.3.6` is npm `latest`. Production (`vivetest.vercel.app`, serving `20f973a`, `next@16.2.4`) also falls in the patched ranges of several other published advisories: seven high-severity ones fixed in 16.2.5, 16.2.6 or 16.2.11 (e.g. GHSA-8h8q-6873-q5fj, Server Components DoS), plus critical GHSA-2xp9-vwfh-vxw4 (< 16.3.3). By my reading the site is probably not exploitable through `next/og`, because its OG and apple-icon inputs are static, nor through AVIF, which is not configured; the DoS advisories are generic to App Router. `git-deploy.md` proposes freezing production on that build "however long [the rebuild] takes". No report noticed this. §4 G2.
3. **The prototype has moved since `prototype.md` was written.** At 02:54 KST: `_hub-body.html` has **0** unfilled `{{J_…}}` placeholders (the report says four), `index.html` was reassembled at 02:53:54 (the report says it is still the round-3 hub), and `j3-forks.html` is 117,023 B, written 02:47:53 (the report gives 116,895 B at 02:19:58). The J workflow finished at 17:53Z. The prototype session told this session it will now run "전체 교차 검증 → 수정 → 아티팩트 최종 게시" and will notify again when published (PROTO-T SendMessage at 17:54:50Z; `peer-handoff-2026-09-26.md`). V4 is **not yet published**, so capture must still wait.
4. **The consent copy asks users to agree to "terms" that do not exist.** `src/messages/kr.json:64` says 「더 나은 경험을 위해 테스트를 진행하기 전에 약관에 동의해 주세요.」 and `en.json:64` says "please agree to the terms", but `src/app` has no terms, privacy or policy route (`git ls-tree` search is empty). The prototype's centred modal reuses this copy verbatim, and the owner made it the mandatory gate before Q2. Friend votes add new data processing on top. No report raised it. §4 G3.
5. **The repository is PUBLIC and has no licence** (`gh repo view`: `"visibility":"PUBLIC"`, `"licenseInfo":null`; no LICENSE/OFL file tracked). This bears directly on where the prototype, the owner's verbatim feedback and the new spec should live. It also bears on the Pretendard OFL obligation and on the personal page `public/56T-ToDo.html`, which production serves right now (`curl` → `200`, 578,500 B). Only `code-assets.md` F7/F8 touches the pieces; nobody put them together. §4 G4.
6. **Five of the reports' own "open questions" are self-decidable from the owner's stated preferences.** The owner needs to decide seven things, not the 13–15 the reports list. None of the seven blocks the foundation step: separation, freeze, prototype capture, skeleton docs and design-system structure. The owner can take all seven as one batch while the foundation proceeds. §5, §6.

---

## 1. What changed after the reports (freshness)

| Item | Report says | State at 02:54 KST | Source |
|:---|:---|:---|:---|
| Owner's carry policy | Not present in any report (all predate it) | 「이전에 구현 레거시는 얽매이지 말고 오로지 프로토타이핑과 요구사항 2개만」; 「기존 레거시에서 여러 누적된 방어코드가 없이」 | PARENT user message 2026-09-25T17:52:19Z; memory `vivetest-rebuild-inputs-are-prototype-and-requirements.md` |
| J stage | "still in flight … Fix/verification stage" (`prototype.md` §0.1) | J workflow `wkzzboaby` completed 17:53:10Z: implementation, two-lens review (15 findings, 7 major), all fixed | PROTO-T task-notification 17:53:10Z; SendMessage 17:54:50Z |
| Hub placeholders | four unfilled (`prototype.md` §0.1) | 0 (`grep -c '{{J_' MOCK/_hub-body.html` = 0) | stat/grep |
| `index.html` | still round 3 (39,989 B) | reassembled 02:53:54, 45,853 B | `ls -l MOCK/` |
| `j3-forks.html` | 116,895 B at 02:19:58 | 117,023 B at 02:47:53 | `ls -l MOCK/` |
| Final artifact | Version 3 published; V4 planned 04:30–05:30 | still V3; the prototype session promises a second notification on publish | SendMessage 17:54:50Z |
| Memory note `vivetest-mobile-interaction-mockups.md` | stale (`prototype.md` §0.3) | still stale; the prototype session says it will update it on final publish | SendMessage |

The plan must treat `prototype.md` §7 ("J final — the live J pass may still change visuals") and §8.1 preconditions as still binding. The trigger for capture is the peer's publish notification, not the clock.

---

## 2. Contradictions between reports

| # | Topic | Report A | Report B | What the evidence says | Resolution for the plan |
|:---|:---|:---|:---|:---|:---|
| X1 | **Carry legacy code into new `main`** | `code-assets.md` §1.7, §9, §10: FOUNDATION = copy the K0 kernel, `scripts/sync`, 29 tests, messages and platform files verbatim with one `git checkout 20f973a -- …` (107 files) | `contracts.md` §8: restate normative rules into new specs; old documents become reference-only. `mess-diagnosis.md` Rule 1: "`main` receives an explicit port list" (each module with its tests) | The owner's 17:52Z follow-up (§0.1) says to look only at the prototype and the requirements, without legacy defensive code | **Owner intent wins.** The manifest becomes a *reference map* (where each requirement is realised in legacy, for lookup via `git show legacy/…:path`). No verbatim copy command in the plan. Self-decidable. |
| X2 | **Carry guards and lessons** | `governance.md` §5.3: carry `helpers/repo.ts`, rewrite `contract-citations`, carry `docs-lifecycle`, `lessons-ledger`, `message-key-parity`, `safe-storage-discipline`, … "day one". §5.8: seed a new ledger with the §2+§3 entries (31 S + 24 M) | `mess-diagnosis.md` Rule 10: "No guard reads Markdown", ≈ 5 bespoke text-reading tests. Rule 6: ledger "capped at ≈ 15 entries" | `contract-citations`, `docs-lifecycle` and `lessons-ledger` all read Markdown, so governance's day-one set violates mess-diagnosis Rule 10 three times. The owner's follow-up rejects 「누적된 방어코드」. Memory note (parent's reading): "가드 테스트·함정 원장(LESSONS)… 이식 대상으로 삼지 않는다" | Carry **knowledge, not artefacts**: the plan author consults the stack lessons (`governance.md` §2) while designing, and each becomes a structural choice (vitest `{ts,tsx}`, preview-mode E2E, `safe-storage` module) rather than a ported guard or ledger entry. Guards are written fresh when their subject exists (L42). Self-decidable. |
| X3 | **Token source format** | `design-system.md` §5.2: option (a) a runtime `src/styles/tokens.css` is the only source; option (c) "a JSON/TS token source generating CSS" rejected "not now" | `mess-diagnosis.md` §7: "TS object → generated CSS + generated literals, because four non-CSS consumers exist". `governance.md` §5.5: "measure first whether the ds token file can be `@import`ed as-is … if not, generate the runtime file from it" | All three agree on a single authored source; they disagree on its format | Self-decidable. Recommendation: **CSS authored** (design-system (a)). Claude Design consumes CSS byte-for-byte, and specimens load it. The per-test palettes, the JS-heavy part, are registry *data*, not tokens (both reports agree). The few non-CSS literals (theme-color meta, bootstrap, icon colours) are generated from the CSS at build time. A TS source would make the pushed CSS a generated file and add a build step before `next dev` for about six colour values. |
| X4 | **Specimens / component gallery** | `design-system.md` §5.4–§5.5: plain-CSS `.vt-*` skins plus hand-written specimen HTML that loads the real CSS; about 40 phone-viewport cards | `mess-diagnosis.md` Rule 8: "a gallery route (dev/build-only) renders every component and state from product code … Specimen HTML … are not written by hand" | The Claude Design pane renders static HTML files (`design-system.md` §5.5: scripts/WebGL in the pane unverified), so a Next gallery route needs a static-export step before push | Self-decidable; the plan must pick one and state the export path. Hybrid option: gallery route → `next build` static export → bundle script. Either way, no hand-restated values. |
| X5 | **Design-system home** | `design-system.md` §5.2: `src/styles/tokens.css`, `src/styles/vt/*.css`, `design-system/specimens/`, `docs/design/system.md`, `.design-bundle/` | `contracts.md` §8 row 7: `docs/design/` (design.md rewritten + `ds/` bundle). `governance.md` §5.4 keeps `docs/design/ds/**` Ask-First "(edits leave the repo via push)" | Three different trees | Self-decidable, but the new `AGENTS.md` Ask-First list must name whatever path is chosen. Otherwise the "edits leave the repo" hazard loses its guard. |
| X6 | **Claude Design target** | `design-system.md` §0, §5.5, §6.1: "Decidable now": create "VIVE Design System v3", freeze `cd630eec` | `mess-diagnosis.md` §7: "Claude Design target … Owner must decide? **Yes** — external account/project" | The owner already asked: 「필요하다면 Claud Design 재연결을 포함해서, Claude Design 전면 재개정도 포함되어야할 것으로 보입니다.」 `create_project` raises its own permission prompt | **Self-decidable.** The request authorises a full revision; the tool prompt is the per-action approval. A new project beats rewriting `cd630eec`, because of the tool's "never as a wholesale replace" contract and the 36 remote-only files. |
| X7 | **Legacy refs** | `mess-diagnosis.md` Rule 1: three phase-end **tags** (`legacy/reference` = `d3305b7`, a P2 tag at `a5aec95`, a P3 tag at `20f973a`) | `git-deploy.md` §3: tag `legacy/gen2-final` **and a permanent branch** `legacy/gen2` at P, optional `legacy/gen1-final`. `governance.md` §5.8: one tag "e.g. `legacy/v2-final`". `contracts.md` BQ-14 row: "a second read-only anchor … at `20f973a` or later" | `a5aec95` verified as the last commit before the mobile analysis (2026-09-11 16:56 +0900; next is `e2aa2fb` 2026-09-14). Naming: the owner's own vocabulary is 1차 (logic+design) / 2차 (design overhaul) / 3차 (mobile **prototyping**). `git-deploy`'s "gen2" lumps the repo's 2차 and step-3 mobile work together, and the owner's 3차 lives outside the repo | Self-decidable. Recommendation: annotated tag + browsable branch at P (the owner navigates GitHub Desktop by branch, and `legacy/reference` is precedent), plus a cheap annotated tag at `a5aec95`. Use ASCII ref names that map onto the owner's vocabulary, e.g. `legacy/phase2-design-final` at `a5aec95` and `legacy/phase3-mobile-final` at P. Each tag message says which 차 it covers and that the owner's 3차 prototype lives in its own ref (S5). |
| X8 | **Sheets sync** | `mess-diagnosis.md` §5: "Sheets sync scripts + `.github/workflows/sync.yml` — carry but disable/retarget first" | `git-deploy.md` §5 and `code-assets.md` §6.3: remove `sync.yml`. `governance.md` §5.5: "`scripts/sync` (if carried) never commits/pushes locally; **only CI pushes**" vs `git-deploy.md` §5: "never a bot push to main"; output lands "through a branch + squash like any other unit" | `sync.yml` has run 209 times with 0 successes (verified). `scripts/sync/sync.ts:155-158` does `git add/commit/push` (verified). `code-assets.md` §10's manifest *includes* `scripts/sync/sync.ts` with that push | Self-decidable: no workflow in new `main`; any future sync script writes files only, and a dispatch/scheduled job lands them through a branch. Whether Sheets stays the content source at all is owner decision O3. |
| X9 | **Fonts** | `code-assets.md` §4.5, §9: LATER, pending the typeface decision | `design-system.md` §6.1, §7 and `mess-diagnosis.md` §5: carry the subset pipeline as-is now | The prototype names Pretendard but never loads it (`prototype.md` §3.6; my `grep @font-face MOCK/shared MOCK/*.html` → none). The owner judged every round in the system Korean font | Self-decidable: keep Pretendard (a prior decision, visually close to Apple SD Gothic Neo), but re-derive the delivery from requirements (subset + no-preload budget). Render the final prototype with Pretendard and compare before freezing type tokens (G6). Add `OFL.txt` (G4). |
| X10 | **Desktop: owner or self?** | `contracts.md` C-1: recommends centred phone column for v1 but classes it C (owner) | `mess-diagnosis.md` §7: "Yes" owner. `design-system.md` §8: "nobody has made" it | The owner never mentioned desktop layout in any round; rounds 2–3 were judged as 「[X] 데스크톱 미리보기」 of the 430px column (DLG:1172, 2106) | **Owner** (O1): a product decision nobody made. Not blocking for the foundation. |
| X11 | **N-ary answers: owner or self?** | `contracts.md` C-2: structure now, scoring semantics for the owner later | `mess-diagnosis.md` §7: "2–4 from day one … No" (not owner). `code-assets.md` §7: "a new scoring mode is a domain design task" | Owner: 「필수는 아니지만, 이 때 활용 가능하도록 유연한 구조가 되거나」 (DLG:2157–2158) | **Structure self-decidable** (ordered answer list 2–4, binary scoring only). Scoring semantics wait for the first real N-ary test (part of O5). |
| X12 | **CI** | `mess-diagnosis.md` §7: CI is an owner decision ("build/deploy configuration") | `governance.md` §5.5 and `design-system.md` §5.1 treat CI/container choices as harness design | `.github/workflows/**` is Ask-First (`AGENTS.md` §4 "빌드·배포·스케줄 설정"); plan approval covers Ask-First. The repo is public, so standard GitHub-hosted runner minutes are free | Self-decidable inside the plan approval. It is not a separate owner question. |
| X13 | **Pixel-baseline environment** | `governance.md` §5.5: "optionally rendered in a Playwright Linux container … needs Docker on the owner's machines → owner decision" | `mess-diagnosis.md` Rule 11: container, same locally and in CI | Running baselines only in a CI container needs no local Docker | Self-decidable: pixel baselines render and compare only in CI; local runs skip `toHaveScreenshot`. Zero owner installs. |
| X14 | **Window implementation** | `prototype.md` §2.1 / l3 realBuild: parallel + intercepting route (`@window/(.)test/[variant]`) | D realBuild (`prototype.md` App. B.4): "같은 문서 안의 공유 요소 전환(View Transitions)". `contracts.md` C-14: "an implementation spike, not an owner decision" | The prototype authors mention both. `contract-citations.test.ts:353-356` throws on `(` and `@` segments (verified), so any route-derivation guard must understand them | Self-decidable spike. Only the requirement is fixed: landing stays mounted, and deep link `/test/{variant}` starts full. |
| X15 | **Owner-decision count** | `contracts.md` §10: 13 open questions. `code-assets.md` §11: 5. `mess-diagnosis.md` §7: 4 "Yes". `governance.md` §7: 6. `git-deploy.md`: 0 recurring | — | Many overlap, and several are already settled by the owner's words (X6, X11, X12, X13) | §5 consolidates them to seven. |

### 2.1 Factual disagreements (counts and paths)

| Claim | Report(s) | Measured | Verdict |
|:---|:---|:---|:---|
| `docs/DECISIONS.md` holds 9 entries | `governance.md` §1.2 | 12 `## ` entries (2026-09-03 ×8, 09-07 ×3, 09-10 ×1) | governance refuted; `mess-diagnosis.md` §3.2 (12) correct |
| 5 local branches ahead of `main` by 1 | `mess-diagnosis.md` §3.5 | 4 (`codex/GS-Sync-postRefactor`, `codex/GS-Sync-preparation`, `codex/backup-main-before-origin-sync-20260513`, `codex/gnb-theme-hover-control`) | mess refuted; `git-deploy.md` §1.5 correct |
| `docs/plans/` has "6 `.md`" active plans | `governance.md` §1.2 | 5 `.md` + the `2026-09-11-mobile-refactor-maps/` folder | governance refuted; `contracts.md` §9.11 ("five") correct |
| `public/theme-bootstrap.js` is a literal home of theme colours | `design-system.md` §2.1 row 5; `governance.md` §1.3 (`theme-color-parity` row) | No such file at HEAD; the literal lives in `src/features/gnb/theme-bootstrap-source.ts` | both refuted; `mess-diagnosis.md` D2 item 11 correct (it is a stale docblock reference) |
| 126 unit test files | `governance.md` §1.3 | 124 `*.test.ts` + 2 helpers = 126 files | reconciled (both right, different denominators) |
| `blocker-traceability.json` 67 vs 68 rows | `contracts.md`/`mess-diagnosis.md` 67; `governance.md` 68 | JSON list of 67 | governance off by one |
| `sync.yml` 40 vs 41 lines | `code-assets.md` 40; `mess-diagnosis.md` 41 | 41 | trivial |
| `GOOGLE_SHEETS_SA_KEY` date | `code-assets.md` 2026-04-27; `git-deploy.md` created 04-02, updated 04-27 | not re-measured | reconcilable |
| Result-page quote location | `prototype.md` DLG:18; `contracts.md` "dialog.md:11" | Line 18 (and 227), inside an assistant summary block quoting the owner, not a verbatim owner message | prototype line right; both correctly treat it as a quoted summary |

---

## 3. Spot-checks of load-bearing claims

| # | Claim (report) | Verdict | Evidence |
|:---|:---|:---|:---|
| 1 | The Sheets sync Action never succeeded: 209 runs, 209 failures (`code-assets.md` §6.3, `git-deploy.md` §1.4) | verified | `gh api repos/vvoohyeon/ViveTest/actions/runs?status=success` → `total_count` 0; all runs → 209 |
| 2 | Production test questions come from in-repo fixtures, not Sheets (`code-assets.md` F2) | verified | `question-bank.ts:2` and `lazy-validation.ts:8` import `getVariantQuestionRows` from `@/features/test/fixtures/questions`; `resolvers.ts` switches registry source on `NODE_ENV === 'production'` (generated) vs fixture |
| 3 | Result commit never clears the active run (`contracts.md` §9.1) | verified | `git grep clearActiveRun HEAD -- src` → only the definition `storage/active-run.ts:101` and the re-export `storage/index.ts:2` |
| 4 | `AGENTS.md` lists `motion@12.34.0`, absent from `package.json` (`contracts.md`, `governance.md`, `mess-diagnosis.md`) | verified | `AGENTS.md:25` has it; `package.json` has no `"motion"`. Also verified: `googleapis` in deps, `@vitejs/plugin-react` present, `vite-node` used by scripts but not declared |
| 5 | vitest collects only `*.test.ts` (L35; `governance.md` §5.5) | verified | `vitest.config.ts` `include: ['tests/unit/**/*.test.ts']` |
| 6 | `contract-citations` throws on route groups / parallel routes, which the window architecture needs (`governance.md` §5.3) | verified | `tests/unit/contract-citations.test.ts:353-356` throws `route group / parallel route 를 이 사상이 다루지 않는다` on `(`/`@` segments |
| 7 | Production = `vivetest.vercel.app` serving `20f973a`; no `vercel.json` ever; fingerprint `63afd6dcdf24fe6d` (`git-deploy.md` §1) | verified | latest Production deployment sha `20f973a…` 2026-09-24T02:49:21Z; `/en` title `ViveTest`, canonical `https://vivetest.vercel.app/en`; fingerprint re-measured `63afd6dcdf24fe6d`; `git log --all -- vercel.json vercel.ts .vercelignore` → 0 |
| 8 | `next/og` ImageResponse flaw in 16.2.0–16.3.5, fixed 16.3.6 (all reports: "unverified") | **verified** (now confirmed) | GHSA-vcvr-r3jv-pc5j, critical, published 2026-09-22, `>= 16.2.0 < 16.3.6 -> 16.3.6`. Exploitable only when attacker-controlled values reach SVG content/attributes/styles in the Node `ImageResponse`, which is exactly what per-question share OG would do. `npm view next dist-tags` → `latest: 16.3.6` |
| 9 | Repo is PUBLIC; `main` unprotected; no rulesets (`git-deploy.md` §1.3) | verified (visibility) | `gh repo view` → `PUBLIC`, `licenseInfo: null`, homepage `https://vibe-test-pearl.vercel.app` (stale, as reported). Protection and rulesets not re-checked |
| 10 | The foundation copy manifest (79 paths → 107 files) all exist at `20f973a` (`code-assets.md` §10) | verified (existence only) | 79 paths, 0 missing, 107 files after expansion. I did not re-run `tsc`/vitest. Now moot per X1 |
| 11 | `req-test.md` §4.3 "next unanswered" and "exactly two options"; `req-landing.md` §1.3 "background animation 0 / tilt off"; `design.md:107/:377` motion bans and `:315` "No A/B letter badges"; `req-landing.md:788` "Mobile: 모달 바텀시트" (`prototype.md` §6, `contracts.md` §3) | verified | read at the cited lines; all texts match |
| 12 | qmbti Q6 `poleA: 'I'` on "learn by talking … with friends" (`code-assets.md` §4.2, `contracts.md` C-10) | verified | `src/features/test/fixtures/questions/qmbti.ts` seq 6: `poleA: 'I'`, `answerA` = 「친구들이랑 같이 이야기하면서 배우는게 좋아」 |
| 13 | Four `anchor/*` tags point at the same commit `afe7077` (`AGENTS.md` §4, `mess-diagnosis.md`) | verified | `w03-06`, `w07-10`, `w11-14`, `w15-17` → `afe7077`; `w01-02` → `66bc50c` |
| 14 | `a5aec95` is the end of phase 2 (`mess-diagnosis.md` §2) | verified | `a5aec95` 2026-09-11 16:56 +0900; next commit `e2aa2fb` 2026-09-14 (mobile analysis) |
| 15 | `public/56T-ToDo.html` is publicly served (`code-assets.md` §4.7) | verified | `curl https://vivetest.vercel.app/56T-ToDo.html` → `200`, 578,500 B; header comment 「배포본 · 개인 데이터 없음」 |
| 16 | EN consent copy "Nothing you answer is sent." (`contracts.md` C-10b) | verified | `en.json:100` |
| 17 | `.env.local` is ignored today (`git-deploy.md` §9) | verified | `git check-ignore -v --no-index .env.local` → `.gitignore:11` |
| 18 | Prototype hub still has four `{{J_…}}` placeholders; `index.html` is round 3 (`prototype.md` §0.1) | **refuted (stale)** | 0 placeholders; `index.html` rebuilt 02:53:54 (§1) |
| 19 | "34 `src` files carry the literal `'A' \| 'B'`" (`code-assets.md` §1.6) | unclear | 17 files match `'A' ?\| ?'B'`, 19 contain both `'A'` and `'B'`, 41 contain `'A'`/`answerA`/`answerChoiceA`/`poleA`. The order of magnitude holds; the exact figure depends on the pattern |
| 20 | `public/theme-bootstrap.js` holds a theme literal (`design-system.md` §2.1; `governance.md` §1.3) | refuted | file absent at HEAD (see §2.1) |
| 21 | `DECISIONS.md` has 9 entries (`governance.md` §1.2) | refuted | 12 |
| 22 | Default stage map `qmbti·egtt → I, energy-check → J`; reduced motion → D; J is 2-answer only (`prototype.md` §2.3) | verified | `MOCK/shared/shell.js:27, 59, 94-95` |

---

## 4. Gaps nobody covered (or covered in one report and dropped from every recommendation)

| # | Gap | Why the plan needs it | Evidence | Recommended handling | Owner? |
|:---|:---|:---|:---|:---|:---|
| G1 | **The seven reports' carry verdicts predate the owner's 17:52Z follow-up** | The plan will otherwise ship a verbatim legacy port the owner just ruled out | §0.1 | Re-label: legacy = requirement lookup only. The plan's "requirements extraction" step reads `req-*.md` normative rules plus the domain facts in code (e.g. locale alias table in `src/config/site.ts`, result payload format) and restates them. Code is re-implemented from the new spec. | No |
| G2 | **Legacy production stays on `next@16.2.4` for an unbounded freeze** | `git-deploy.md` §4 freezes production on P "however long" the rebuild takes. P is below patched versions of several published advisories, including high-severity App Router DoS (GHSA-8h8q-6873-q5fj, `< 16.2.5`) and others fixed in 16.2.5/16.2.6/16.2.11. The `next/og` RCE is probably not reachable (static inputs), and AVIF optimisation is not configured | `gh api repos/vercel/next.js/security-advisories` (§3 #8) | Before the cutover, land a security-only bump of `next` (to `16.3.6`, or at minimum `16.2.11`) on the **legacy** `main`. Verify its production build succeeded, then take P = that commit, so the frozen build is patched. Ask-First (`package.json`) and a production deploy, so it rides the plan approval. No extra owner step beyond that. The rebuild pins `next >= 16.3.6` from its first dependency commit | No (plan approval) |
| G3 | **No terms / privacy policy, yet the mandatory gate asks users to agree to terms** | The owner made the instruction+consent modal the key legal gate (「아주 중요한 고지의 메시지」). Sharing/votes add server-side processing. Korean PIPA and GDPR (12 locales, EU included) expect a reachable privacy notice. The consent note links to nothing | `kr.json:64-65`, `en.json:64-65`; no `privacy/terms/policy/legal` route under `src/app` | Add a privacy/terms surface to the requirements set; the agent drafts it. Operator identity, contact and jurisdiction exist only in the owner's head → O6 | Yes (O6) |
| G4 | **Public repository exposure and licensing** | The new spec will quote the owner's feedback verbatim; the prototype sources and BRIEFs describe unreleased design; all become public if committed. No repo LICENSE exists, so the code is "all rights reserved" by default. Pretendard (OFL 1.1) is served without its licence text. `56T-ToDo.html` is a personal page on the product domain | `gh repo view` (§3 #9); `code-assets.md` F7/F8; §3 #15 | Owner picks visibility and licence (O7). Regardless of that choice: add `OFL.txt` beside any served font, and leave `56T-ToDo.html` out of the new `main` (it disappears from production at launch; if the owner still uses it, host it elsewhere) | Yes (O7) |
| G5 | **Prototype capture: what the artifact does not hold, and the deadline** | The artifact holds 29 web files. `BRIEF.md`/`BRIEF2.md`/`BRIEF3.md` (the only written specs of each round), `tools/*` (the executable window-protocol spec) and the regression scripts in `shots/` exist **only** in `/private/tmp`, which is lost on reboot or cleanup. `prototype.md` §8 lists them but nobody assigns a trigger or a home | `ls MOCK/tools` (6 files), `MOCK/shared` (9); `prototype.md` §8.2–§8.4 | Trigger: the peer's publish notification. Home: a dedicated prototype ref outside `main`'s tree (see S5), copied from `MOCK/` (not moved), excluding `shots/*.png`, `jdir/`, `_r3-backup/`. Also record the artifact URL + version label. Patch the absolute `node_modules/playwright` path in `tools/*.mjs` in the captured copy or document it | No |
| G6 | **What the owner approved was never rendered with Pretendard, in light mode, or on a phone in rounds 2–3** | Rounds 2–3: 「본 환경: [ ] 폰 [X] 데스크톱 미리보기」, 「테마: … [X] 다크」 (DLG:1172–1173, 2106–2107). Pretendard is named but never loaded. The type roles use `line-height` 1–1.05, which v2 measured to clip Pretendard (`design-system.md` §3.4, §8) | §3 #X9 | Before freezing type/colour tokens, the agent renders the final prototype with Pretendard in light and dark at 360/390/430 widths in WebKit and Chromium, measures clipping and contrast, and shows the owner only the diffs that change the look. This rides the owner's review of the first preview; no separate owner task | No (agent check; owner sees results at preview) |
| G7 | **Content and translation ownership for 12 locales** | Catalog/question content exists only in `en`+`kr` (`code-assets.md` §1.5). The prototype is Korean-only. New strings (share sheet, 「나 대신 골라 줘」, vote line, stage labels, the modal) need 12-locale copy. Nobody names who translates, who reviews, or which locales launch with real content. `design-system.md` §5.5 notes the per-character kinetic title breaks Hindi graphemes | `code-assets.md` §4.2–§4.3; `prototype.md` §4.4, §7.15 | The agent translates UI chrome (message catalogs, key parity) with a documented glossary. Test content per locale and the launch locale set are part of O3 | Partly (O3) |
| G8 | **Telemetry sink and analytics are zero today** | `/api/telemetry` validates and drops every event (`code-assets.md` F5). Whether Vercel Analytics/Speed Insights are enabled on the Vercel project is unknown (dashboard, not repo). Retention (REQ-F-017) has been undecided since inception. Share/vote events need a sink. Only `code-assets.md` asks "where should telemetry … be stored?" | `code-assets.md` §6.2, §11.5; `contracts.md` §4.4 | Bundle with the vote backend decision (O2): one persistence choice serves votes and telemetry | Yes (O2) |
| G9 | **Share-link domain** | The prototype's share URL is the fake `https://vivetest.app/v/…` (`prototype.md` §4.3). Real links would be `vivetest.vercel.app/…`: long, and not brandable in KakaoTalk/Instagram previews. A custom domain is a purchase | `share.js:443` per `prototype.md`; `git-deploy.md` §1.1 | Part of O2 (purchase = owner) | Yes (O2) |
| G10 | **Vercel plan terms and quotas** | `git-deploy.md` §10 infers Hobby from the team slug. Hobby is limited to non-commercial use and has tighter function/storage quotas; a vote API plus per-question OG image generation adds function load. Unknown whether monetisation is planned | `git-deploy.md` §10 | Fold into O2 (infra and cost) | Yes (O2) |
| G11 | **Links already shared in the wild must keep working after launch** | Result URLs are self-contained (`/{locale}/result/{variant}/{type}?{base64}`, `req-test.md` §5.1) and "shareable/revisitable without account" (`requirements.md` §1). Legacy production has served them since at least 2026-09-16. A from-scratch rewrite that changes the payload codec, type segment or route breaks every link people already posted. Storage keys (consent `vivetest-telemetry-consent`, theme) have the same continuity issue for returning users; only `contracts.md` §10.11 raises storage keys | `contracts.md` §4.1, §4.6, §10.11 | Add a "compatibility surface" requirement: result route, payload codec and type segment accepted forever (decode old, may emit new); consent/theme keys read-compatible. Self-decidable from the requirements the owner said must be kept | No |
| G12 | **Project memory will mislead sessions on the new `main`** | Memory is keyed by project basename and synced to the other machine. After cutover, notes such as `vivetest-preexisting-red-gates.md` (123-gate expectations), `vivetest-qa-rules-is-red-by-bq07-deferral.md`, `vivetest-harness-guards-live-in-vitest.md`, `vivetest-ds-readme-findings-are-stale.md`, `vivetest-baseline-regen-preapproved.md`, `vivetest-parallel-design-sessions-land-fast.md`, `vivetest-checkpoint-branches-are-local-only.md` and `vivetest-claude-design-integration-status.md` describe a tree that no longer exists but still load into every session | memory folder listing (16 notes) | The cutover unit retires or rewrites these (legacy-scoped wording or deletion via the memory repo), and adds one note naming the legacy refs. The prototype session will also rewrite `vivetest-mobile-interaction-mockups.md` on publish, so sequence after that to avoid a race | No |
| G13 | **The other machine (SDS-MacBook-Pro)** | Its primary checkout will pull a tree-replacing commit. Its local-only branches are unknown (`git-deploy.md` §1.5 can see only this machine). Its `.env.local` survives only if the new `.gitignore` keeps ignoring it | `git-deploy.md` §1.5, §9 | The plan states the expected result of the next GitHub Desktop pull on each machine (tracked files replaced, ignored files kept) and keeps `.env*.local` ignored from the first commit. Any machine-local branch audit is done by an agent session on that machine, not by the owner | No |
| G14 | **WCAG 2.2.2 for the always-moving landing field** | The owner removed hold-to-pause (「굳이 필요하지 않습니다」, DLG:587), and prototype D raised 2.2.2 (`prototype.md` §2.15, App. B.9) | `prototype.md` §7.6 | Self-decidable within the owner's principles (no new controls): the field settles after ≤ 5 s idle and moves only in response to deck interaction, and reduced motion stops it entirely. This satisfies 2.2.2 without a pause button | No |
| G15 | **Performance budget for WebGL field + canvas stages on low-end phones** | Raised as C-12 in `contracts.md`; nobody proposes numbers | `contracts.md` C-12; `prototype.md` §3.8 | Self-decidable: set a JS/GPU budget and a measured fallback rule (CSS gradient when `failIfMajorPerformanceCaveat` or low memory), verified on the mobile-WebKit/Chromium device projects | No |
| G16 | **Error monitoring** | No error reporting exists; the new stages are canvas/WebGL-heavy and history-owning. Production errors would be invisible | none in repo | Optional; adding a vendor needs an account, so defer until O2's infra choice | Later |
| G17 | **Example/demo data must not leak into product** | `demo-three`/`demo-four`, `예시` chips, simulated vote counts and `category` mockup choices (`prototype.md` §4.1, §1.7) are prototype-only | `prototype.md` §1.7, §4.1 | The requirements extraction marks each as non-product. Self-decidable | No |
| G18 | **An unused credential remains** | If sync is removed, `GOOGLE_SHEETS_SA_KEY` stays in Actions secrets with read-only Sheets scope, unused. Harmless, but it is an unowned credential | `git-deploy.md` §5 | Keep until O3 decides Sheets; if Sheets is dropped, the owner deletes the secret and service account (credential handling is the owner's). One action, once | Only if Sheets dropped |

### 4.1 Coverage of the specific topics the task named

| Topic | Covered by | Critic verdict |
|:---|:---|:---|
| Fonts licensing | `code-assets.md` F7 only (no OFL file) | Real (verified: no licence file tracked). Missing from every plan-level recommendation → G4 |
| Messages ownership | `code-assets.md` §4.3 (keys, reuse); nobody on translation/ownership | Gap → G7 |
| Public repo exposure | `git-deploy.md` §9 (`.env.local` only) | Gap → G4 |
| OG images | `contracts.md` C-7, `code-assets.md` §4.6, `prototype.md` §7.4 | Covered; advisory now verified (§3 #8); legacy exposure G2 |
| Telemetry backend | `code-assets.md` F5 | Under-covered → G8 / O2 |
| Analytics | `contracts.md` §4.7, `code-assets.md` §6.4 (consent gates) | Whether Vercel Analytics is enabled at all is unknown → G8 |
| Secrets in GH Actions | `git-deploy.md` §1.3, §5; `code-assets.md` §6.3 | Covered; residual G18 |
| Sheets data owner | `code-assets.md` §11.1 asks; `contracts.md` §8 assumes Sheets | Open → O3 |

---

## 5. Decisions that genuinely need the owner (seven)

Each has at least one of: a product/UX choice nobody has made, money or an external account, legal identity, or information only the owner holds. **None blocks the foundation step**: separation, production freeze, prototype capture, skeleton contract docs and design-system structure can all proceed on the self-decidable items in §6. The plan can present all seven in one batch; the first decision's answer is needed only when the corresponding surface is built.

| # | Decision | Why the owner | Options (recommendation marked) | Blocks | Recurring owner actions in the recommended path |
|:---|:---|:---|:---|:---|:---|
| O1 | **Desktop/tablet presentation for v1** | Product/UX; the owner never judged a wide layout (X10) | (a) **Recommended**: the phone app centred as a column on wide screens, with keyboard/pointer parity and no hover-only paths. Like a phone-sized shop window on a big street: same shop, framed. (b) A designed wide layout (needs a prototype round). (c) Port legacy desktop behaviour (contradicts the owner's follow-up) | Landing/shell layout units | 0 |
| O2 | **Sharing + votes at launch, and the infrastructure behind them** | Money/accounts (storage vendor, optional custom domain), privacy basis, scope | (a) **Recommended**: in-test "ask a friend" link and friend vote page at launch, backed by one managed store (via the Vercel Marketplace) that also becomes the telemetry sink; votes treated as functional data (anonymous, deduplicated, with an expiry). A custom short domain is optional. (b) Share link and 9:16 card at launch, votes in v1.1 (no backend yet). (c) No sharing at launch. The owner called sharing the viral core: 「테스트 도중에도 자연스럽고 꼭 필요합니다」, votes 「있어야 한다」 | Share/vote units; per-question OG | 0 after one-time setup (vendor account and purchase approvals are one-time) |
| O3 | **Content source, content correctness, launch locales** | Only the owner knows the intended content, and the owner is the editor of any CMS | Source: (a) **Recommended**: repo-owned content files, with Sheets import as an optional later tool (the sync never ran and questions never reached runtime, `code-assets.md` F1–F2). (b) Keep Sheets as the CMS and build a real two-artifact pipeline. Content: qmbti Q6 pole, `energy-check`'s real scoring model (it reuses the MBTI schema), placeholder subtitles, the three DevOps "blog" articles, results content (none exists). Locales: which of the 12 launch with translated test content vs English fallback | Content/registry units and launch | 0 in (a) |
| O4 | **Launch bar and the surfaces the prototype never drew** | The owner deferred the result page and post-submit transition: 「구체적인 피드백은 테스트 플로우 최종 확정 후, 결과 페이지까지 이어지는 부분의 피드백을 추가로 나중에 제공하겠습니다.」 (DLG:631). Stage-per-test values: 「완성된 후에는 각 테스트 별로 어떤 타입을 적용할지를 사전에 정의하겠습니다.」 (DLG:2200) | (a) **Recommended**: one more prototype round, run by an agent, covering result page, menu/locale/theme, consent banner, history, blog, 404/recovery in the established language. The owner reviews once, then the launch bar = those surfaces + stage map. (b) The agent designs those surfaces directly in product code and the owner reviews previews. (c) Launch without them (not viable: result/consent/recovery are requirements) | Result/blog/history/menu units; launch | 1 review round (a decision, not labour) |
| O5 | **Residual prototype-vs-requirement conflicts** | The owner said the build must carry the legacy requirements; these legacy rules conflict with each other or with the prototype, and the owner has not ruled on them | Batch, each with a recommendation: history scope, list-only (`req-test.md` §8.4) vs share URL + delete (REQ-F-012) → **list-only for v1**; ≥ 5 s result "derivation loading" (`req-test.md` §4.6, never implemented) → **decide with O4's result round**; N-ary scoring semantics → **defer until a real 3/4-answer test exists** (structure ships now, S9); unavailable card tap: toast (prototype) vs inert (BQ-10) → **toast** (feedback without entry) | Domain spec sections | 0 |
| O6 | **Legal identity: terms / privacy notice** | Operator name, contact and jurisdiction exist only in the owner's head; the consent copy promises 「약관」 (G3) | (a) **Recommended**: the agent drafts a short privacy notice + terms, the owner fills 2–3 identity fields and approves; the consent note links to it. (b) Change the copy to "analytics consent" wording with no terms (still needs a privacy notice for votes/telemetry) | Consent/modal unit, votes | 0 after one approval |
| O7 | **Repository visibility and licence** | Exposure of the owner's feedback, the unreleased design and any personal pages; licence choice is the owner's | (a) **Recommended**: keep the repo public (free CI minutes), add a LICENSE of the owner's choice, keep prototype captures and verbatim feedback in a private place (the artifact, or a private companion repo) and cite them from the public spec. (b) Make the repo private (Actions minutes then count). Also: is `/56T-ToDo.html` still needed anywhere? | Where the prototype capture and spec quotes live | 0 |

Items the reports list as owner decisions that are **not** in this list, and why: Claude Design target (X6, authorised by the request); CI (X12, plan approval); pixel-baseline container (X13, CI-only); N-ary *structure* (X11, owner's words settle it); token source format (X3, technical); deleting the 11 stale local branches and 4 misleading anchor tags (not needed for the rebuild; leave untouched, so no decision arises); Sheets secret deletion (only if O3 drops Sheets).

---

## 6. Decisions the plan can make itself, and the stated preference that settles each

Stated preferences used: maintainability (「추후 운영 시 유지보수가 거의 불가능해집니다」), from-scratch cleanliness (17:52Z follow-up), zero manual work (global rules), mobile-first prototype direction (「3개안 … 모두 이 서비스 안에서 사용하겠습니다」), highest quality (「시간이 아무리 오래 걸려도 좋으니 최고의 품질 하나만 고려해서」, DLG:2408).

| # | Decision | Choice | Settled by |
|:---|:---|:---|:---|
| S1 | Legacy carry policy | No verbatim code, test, guard or ledger port. Legacy refs are the lookup for "what was required". `code-assets.md` §10 becomes a reference map | 17:52Z follow-up |
| S2 | How `main` restarts | One tree-replacing squash commit on top of history; no orphan root, no force push; legacy tag(s) + a browsable branch at P created in the same `--atomic` push (`git-deploy.md` §6.3). Names follow the owner's 1차/2차/3차 vocabulary | maintainability; global landing rules |
| S3 | Production during the rebuild | Freeze via `vercel.json` `git.deploymentEnabled.main=false` + production `ignoreCommand`. Rehearse both on a probe branch first (`git-deploy.md` §6.2); a unit test asserts the freeze keys. **Patch legacy `next` before taking P** (G2) | zero manual work; not launching a half-built app |
| S4 | `sync.yml` | Absent from new `main`; secrets kept until O3 | 209/209 failures |
| S5 | Prototype capture | After the peer's publish notification: copy (never move) `MOCK/` sources + BRIEFs + `tools/` + regression scripts, excluding `shots/*.png`, `jdir/`, `_r3-backup/`, into a dedicated prototype ref outside `main`'s tree (e.g. an orphan branch `prototype/mobile-final` + annotated tag), unless O7 moves it to a private home. Record the artifact URL + final version label in the new spec | 「기존 저장소 구현, 프로토타이핑 결과물을 분리하고」 |
| S6 | Requirements extraction | `contracts.md` §8 seven-document SSOT set (domain, platform, privacy-telemetry, content pipeline, experience, sharing, design system), normative rules only, stable IDs, no precedence chains, no restated counts (`mess-diagnosis.md` Rules 4–5), plus the compatibility surface (G11) and a legal surface (G3) | 「기존 레거시의 요구사항을 담아야합니다」 |
| S7 | Design system | One authored CSS token source consumed by the runtime (X3); palettes as registry data with contrast-invariant unit tests; non-CSS literals generated; new Claude Design project "VIVE Design System v3" built from a generated bundle; `cd630eec` frozen as the v2 record | `design-system.md` §5; request authorises the revision |
| S8 | Bans lifted / kept | Lift: overshoot springs, parallax, living background, saturated per-test colour, A/B badges, colour-world first screen, glass on worlds, dashed lock/soon cards. Keep: no drag-to-close, 44 px targets, no tiny dot navigation, no emoji/exclamation in UI chrome | owner verdicts rounds 1–3 (`design-system.md` §4.2) |
| S9 | Answer model and stage data | Ordered answers 2–4 in schema, preview and telemetry from day one; binary scoring only; `stage` and palette as per-variant data with defaults, values supplied by the owner at launch | 「유연한 구조」; 「최종 배포 시점에」 |
| S10 | Revisit rule | Advance exactly one question after re-answering a revisited question | owner decision 「되돌아간 문항부터 한 단계식 이동」 (DLG:1286) |
| S11 | Instruction + consent | Centred modal for all stages; the full consent × attribute × ingress matrix of `req-landing.md` §13.5 / `req-test.md` §3.6, including deny persistence and `OPTED_OUT` paths the mockup did not model | round 3 (DLG:2132–2133); requirements |
| S12 | Harness | One gate (lint → typecheck → unit → build → E2E); vitest `**/*.test.{ts,tsx}`; Playwright device projects with `mobile-webkit` primary; preview server mode; few pixel baselines rendered only in a CI Linux container; GitHub Actions CI | L05/L24/L28/L32/L35 knowledge; public repo = free minutes |
| S13 | Docs governance | `AGENTS.md` ≤ ~150 lines with derived facts only; fresh decision log with a new ID prefix and no change-history table; lifecycle convention kept (recursive); Korean-first as today | `governance.md` §5, `mess-diagnosis.md` Rules 4–6 |
| S14 | Typeface | Pretendard Variable with v2's 12-locale fallback stack; re-derive subset delivery; add `OFL.txt`; verify by render (G6) | prior decision; visual similarity to what the owner reviewed |
| S15 | Mobile-first construction | No hover-only or desktop-shell code before O1 | 「모바일 포커스」; `mess-diagnosis.md` Rule 12 |
| S16 | WCAG 2.2.2, performance budget, compatibility surface, memory hygiene | G14, G15, G11, G12 as recommended | owner principles; requirements kept |
| S17 | Stale refs | Leave the 11 stale local branches and the `anchor/*` tags untouched; the new `AGENTS.md` names only the refs a session needs | deletion needs approval and gains nothing |

---

## 7. Sequencing constraints the plan must respect

1. **Capture waits for V4 but must beat `/tmp` cleanup.** The capture trigger is the prototype session's publish notification (G5). Until then the plan can build everything that does not quote final J values.
2. **Patch legacy production before choosing P** (G2): legacy bump → production build `success` → re-measure the fingerprint → P.
3. **Probe the freeze before the cutover** (`git-deploy.md` §6.2). It is the only unverified Vercel behaviour.
4. **Cutover atomically**: squash S with `vercel.json` + legacy tag/branch at P in one `--atomic` push. Verify in separate commands: no Production deployment for S, fingerprint unchanged, 0 Actions runs.
5. **Memory retirement after the peer's final memory update** (G12), then push the memory repo.
6. **Handoff split per global rules**: a plan another session executes goes into 2–3 ordered documents. A natural split is step 1 (separation: legacy patch, freeze probe, prototype capture, cutover, memory hygiene), step 2 (requirements extraction into the SSOT set + design-system v3 + Claude Design project), step 3 (application skeleton: routes incl. parallel/intercepting window, shell, one stage end-to-end). The seven owner decisions go to the owner as one batch alongside step 1.

---

## 8. Per-report reliability notes

- `prototype.md`: thorough and well cited; the live-state paragraphs are stale by ~30 minutes (§1). Its capture manifest is the best available but misses nothing important.
- `contracts.md`: the most useful for the owner's new "requirements only" framing. Its classification (A/B/B0/C/D) is the right extraction key. Its counts are correct where I checked.
- `code-assets.md`: measurements verified (manifest existence, sync failures, fixture-sourced questions). Its central recommendation (verbatim FOUNDATION copy) is overtaken by the owner's follow-up. Its §3.7 debt list and §8 findings (F1–F13) remain valid as "do not repeat" inputs.
- `design-system.md`: strong on structure and on the Claude Design tool contract. It wrongly lists `public/theme-bootstrap.js`. It did not call `DesignSync`, so this machine's authorisation state is unverified (first `list_projects` call decides it).
- `governance.md`: an excellent stack-lesson catalogue. Two counts are wrong (DECISIONS 9 → 12, plans 6 → 5), and it repeats the `theme-bootstrap.js` error. Its carry-guards/ledger plan conflicts with `mess-diagnosis.md` Rule 10 and with the owner's follow-up.
- `mess-diagnosis.md`: diagnosis verified in the places checked. One count is wrong (5 → 4 local branches with unique commits). Its "owner must decide" column over-escalates (Claude Design, CI).
- `git-deploy.md`: the most operationally precise; every checked fact holds. It misses that the frozen P is on a Next version with published advisories (G2).

## 9. Sources consulted by the critic

Reports: the seven files above + `peer-handoff-2026-09-26.md`. Repo (`20f973a`, read-only): `AGENTS.md:13,25`; `package.json`; `vitest.config.ts`; `.claude/settings.json`; `.gitignore` (via `check-ignore`); `docs/DECISIONS.md` headings; `docs/plans/` listing; `docs/blocker-traceability.json`; `docs/req-test.md:478, 676-684`; `docs/req-landing.md:13-21, 788`; `docs/design/design.md:107, 315, 377`; `src/messages/{kr,en}.json:64-65,100`; `src/features/test/{question-bank,lazy-validation}.ts`; `src/features/test/storage/*` (`git grep clearActiveRun`); `src/features/variant-registry/resolvers.ts:60-85`; `scripts/sync/sync.ts:145-165`; `tests/unit/contract-citations.test.ts:345-362`; `src/features/test/fixtures/questions/qmbti.ts` seq 6; `src/app/[locale]/opengraph-image.tsx`, `src/app/apple-icon.tsx` (`next/og` use); `next.config.ts` (no `images`); `public/56T-ToDo.html` head; refs via `for-each-ref`, `tag -l`, `rev-parse`. External (GET only): `gh repo view vvoohyeon/ViveTest`; `gh api …/actions/runs`; `gh api …/deployments?environment=Production`; `gh api repos/vercel/next.js/security-advisories` (+ details of GHSA-vcvr-r3jv-pc5j, GHSA-2xp9-vwfh-vxw4, GHSA-6gpp-xcg3-4w24); `npm view next dist-tags`; `curl https://vivetest.vercel.app/en` and `/56T-ToDo.html`. Transcripts (read-only): PARENT user messages; PROTO-T tail (task notification, SendMessage). Prototype scratchpad (stat/grep only): `MOCK/` listing, `_hub-body.html` placeholder count, `shared/shell.js:27,59,94-95`, `@font-face` search. Memory: folder listing and `vivetest-rebuild-inputs-are-prototype-and-requirements.md`.
