# Claude Code — ViveTest 코딩 세션

**이 저장소의 계약 정본은 아래 `AGENTS.md` 하나다.** 루트 `CLAUDE.md`는 존재하지 않으며, 그것은 누락이 아니라 설계다 — ViveTest에는 런타임 에이전트도 실행 파이프라인도 없어 계약을 달리 읽어야 할 두 번째 독자가 없고, 도구별 사본은 같은 규칙을 두 곳에 두어 표류시키기 때문이다. Codex도 같은 `AGENTS.md`를 읽는다. 이 파일은 포인터일 뿐이므로 여기에 규칙을 적지 않는다 — 저장소 규칙이 필요하면 `AGENTS.md`에 적고, 왜 그 모양인지는 `docs/DECISIONS.md`에 적는다.

*how to work*(워크스페이스·git 절차·plan mode·명확화·검증 절차·승인 판단)는 전역 `~/.claude/CLAUDE.md`의 몫이다. `docs/plans/**`와 `docs/archive/**`는 완료된 작업의 역사 기록이며 지시가 아니다 — 그 문서들이 서술하는 wave별 워크트리 토폴로지는 2026-09-03에 철거됐고, 현행 workspace·브랜치 규칙은 `AGENTS.md` §4가 갖는다.

@../AGENTS.md
